import { GraphQLType } from 'graphql';
import { Thunk } from "../../../types";
/**
 *
 * @param input
 * @param allowThunk
 * @param preferInputType We want to be able to have single class used both for output and input type - thats why we need to be able to set resolve priority in different scenarios
 */
export declare function resolveType(input: any, preferInputType?: boolean, allowThunk?: boolean): GraphQLType;
export declare function resolveTypesList(types: Thunk<any[]>, preferInputType?: boolean): GraphQLType[];
